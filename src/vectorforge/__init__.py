"""VectorForge: a distributed vector search engine built from scratch.

Implements HNSW (Hierarchical Navigable Small World) indexing, persistence,
a FastAPI + gRPC query layer, consistent-hash based sharding, and a
recall/latency benchmarking suite.
"""

__version__ = "0.1.0"
